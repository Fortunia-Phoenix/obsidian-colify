{
	"name": "colify",
	"version": "0.1.5",
	"description": "A visual column layout editor for Live Preview.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs --watch",
		"test": "node scripts/core-regression.mjs",
		"build": "tsc --noEmit && node scripts/core-regression.mjs && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"columns",
		"live-preview"
	],
	"author": "Qismet",
	"license": "MIT",
	"devDependencies": {
		"@codemirror/state": "^6.5.0",
		"@codemirror/view": "^6.38.6",
		"@types/node": "^20.11.30",
		"esbuild": "^0.20.2",
		"obsidian": "^1.5.12",
		"typescript": "^5.4.3"
	}
}
